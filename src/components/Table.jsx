import React from 'react';
import fetchApi from '../services/SWPApi';

const data = fetchApi;
console.log(data);

const Table = () => {
  return (
    <div>
      Dados
    </div>
  );
};

export default Table;
