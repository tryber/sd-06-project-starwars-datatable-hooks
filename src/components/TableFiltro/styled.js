import Styled from 'styled-components';

const styledFilter = Styled.div`
  width: 100%;
  background:  #D3D3D3;
  height: 100px;

    input {
      margin: 30px 10px
    }
`;

export default styledFilter;
