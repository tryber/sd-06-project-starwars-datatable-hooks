import { createContext } from 'react';

const StarWarsContext = createContext();

// primeiro passo criar o context
// criando uma caixinha que salvamos na variável
// StarWarsContext que armazena o state para toda a aplicação
// Depois disso, criar o Provider.js (nada mais é que um componente)

export default StarWarsContext;
